server:
  port: 8081

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/bank-client
    username: user
    password: password
    hikari:
      poolName: Hikari
      auto-commit: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
